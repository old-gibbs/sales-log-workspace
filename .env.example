# Neon Postgres（Vercel/Neon Native Integration が Production / Preview 環境向けに自動設定）
# ローカル開発は `vercel env pull .env.local --environment=production` で取得する
# ※ 第7回課題スコープでは Production DB をローカルからも触る想定。本格運用なら Neon ブランチを分ける
DATABASE_URL=
DATABASE_URL_UNPOOLED=
